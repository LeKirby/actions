version: "3.4"

services:
  php-custom:
    image: curlimages/curl
    networks:
      - network1
    build: .
    command: curl 172.18.0.2:80
    tty: true

networks:
  network1:
    name: $NETWORK
    external: true
