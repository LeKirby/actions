version: "3.4"

services:
  php-custom:
    image: alpine
    network_mode: host
    networks:
      - network1
    build: .
    command: nslookup test-server
    tty: true

networks:
  network1:
    name: $NETWORK
    external: true
